language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-prskk-gulp-browserify-react' ]; then cd .. && eval "mv $currentfolder generator-prskk-gulp-browserify-react" && cd generator-prskk-gulp-browserify-react; fi

